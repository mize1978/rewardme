<%# 通知メッセージ %>
<div class="notice-wrapper">
  <% if notice %>
    <p class="notice"><%= notice %></p>
  <% end %>
</div>

<%# ページタイトル %>
<h1>RewardMe 🍰</h1>

<%# カレンダー %>
<%= month_calendar events: @done_tasks do |date, tasks| %>
  <%= date.day %>

  <% tasks.each do |task| %>
    <div class="calendar-task">🍰</div>
  <% end %>
<% end %>

<%# 説明文 %>
<p class="app-description">
  がんばった自分をほめるためだけの🩵
</p>

<div class="badge-box">
  🏅 今日の称号：<%= current_user.badge %>
</div>

<%# 今日のがんばり数（←ここ重要） %>
<div class="achievement-box">
  🎉 今日のがんばり数：<%= @today_done_count %> 個
</div>
<div class="achievement-box">
  🔥 連続がんばり日数：<%= @streak_days %> 日
</div>

<%# 未完了タスク %>
<h2>🔥 がんばること</h2>

<div id="not-done-tasks">
  <% @not_done_tasks.each do |task| %>
    <div class="task-card <%= "priority-#{task.priority}" %>">
      <%= task.title %>
      <p>優先度：<%= task.priority %></p>

      <%= button_to "完了", task_path(task, task: { done: true }), method: :patch %>
      <%= button_to "削除", task_path(task), method: :delete %>
    </div>
  <% end %>
</div>

<%# 完了タスク %>
<h2>🍰 ごほうびタイム</h2>

<div id="done-tasks">
  <% @done_tasks.each do |task| %>
    <div class="task-card done <%= "priority-#{task.priority}" %>">
      <%= task.title %>

      <p>🩵 えらいね〜 🩵ごほうびタイム🧁🍰</p>

      <%= button_to "戻す", task_path(task, task: { done: false }), method: :patch %>
      <%= button_to "削除", task_path(task), method: :delete %>
    </div>
  <% end %>
</div>

<%# 新規作成ボタン %>
<%= link_to "＋ チャレンジ 🍰", new_task_path, class: "new-task-button" %>